이 폴더(`sources`)를 그대로 Chrome 확장프로그램 > 개발자 모드 > 압축해제된 확장 프로그램 로드에서 선택하세요.

이 확장의 유일한 manifest는 `sources/manifest.json`입니다.
저장소 루트에는 manifest를 두지 않습니다. 루트 manifest를 두면 `sources/` 기준 경로와 섞여 잘못된 파일이 로드될 수 있습니다.

고정 규칙 문서는 `docs/BADGE_AND_STEERING_RULES.md`에 있습니다.
